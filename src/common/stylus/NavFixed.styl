Nav-fixed($fx)
  .footer-nav
	width 100%
	position fixed
	$h 0
    .nav
      display flex
      flex-direction row
      align-items center
      text-align center
      width: 100%
      height 40px
      font-size 12px
      li
        flex 1
        .tab
          display flex
          flex-direction column
          &.active
            color blue